{% extends '@View/views/base.html.twig' %}



{% block body %}

<!-- Page Content -->
<div class="container">

    <!-- Introduction Row -->
    <div class="row">
        <div class="col-lg-12">

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                <h1 class="page-header">Modifier cet Ami
                </h1>

            {% endif %}

        </div>
    </div>

    <!-- Team Members Row -->
    <div class="row">



        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success" type="submit" value="Modifier les informations" />
        <a href="/delete/{{friend.id}}/{{app.user.id}}" class="btn btn-danger">Supprimer cet ami</a>
        {{ form_end(edit_form) }}
    </div>

</div>

{% endblock %}



