{% extends '@View/base.html.twig' %}
{% block title %}{% endblock %}

{% block body %}

    <div class="row">
        <fieldset>
            <legend align="left"><div id="fieldtitre"> <h3>Liste des Partenaires Choisi</h3></div></legend>
            <table border="1" CELLSPACING="0" CELLPADDING="8">
                <thead>
                <tr>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Nom d'utilisateur</th>
                    <th>Pays</th>
                    <th>    </th>
                </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.lastName }}</td>
                        <td>{{ user.firstName }}</td>
                        <td>{{ user.username }}</td>
                        <td>{{ user.pays|capitalize }}</td>
                        <td>    </td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('delete', { 'id': app.user.id }) }}">Supprimer</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </fieldset>
    </div>





{% endblock %}